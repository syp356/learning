package com.syp.pattern.builder;

/**
 * 2020-03-31
 * 建造者模式：
 * 将一个复杂对象的构建与他的表示分离，使得同样的构建过程可以创建不同额表示
 * 特征：
 * 用户只需要指定需要建造的类型就可以获得对象，建造过程及细节不需要了解
 *
 * 创建型模式
 *
 * 适用场景
 * 1.创建对象需要很多步骤，但是步骤顺序不固定
 * 2.如果一个对象有非常复杂的内部结构（很多属性）
 * 3.把对象的创建和使用分离
 *
 *源码案例
 * StringBuilder.append();
 *
 *
 *
 */
public class Builder {
}
